# Template `{{ properties.path }}`

Revision `{{ properties.revision }}`, characters {{ properties.offset }}–{{ properties.next_offset }} of {{ properties.total_characters }}.

```text
{{ properties.content }}
```
{% if follow_ups %}Use the supplied typed continuation to read the remaining template content.{% endif %}
