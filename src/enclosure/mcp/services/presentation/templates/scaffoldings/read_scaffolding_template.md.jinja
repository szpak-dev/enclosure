# Template `{{ data.path }}`

Revision `{{ data.revision }}`, characters {{ data.offset }}–{{ data.next_offset }} of {{ data.total_characters }}.

```text
{{ data.content }}
```
{% if follow_ups %}Use the supplied typed continuation to read the remaining template content.{% endif %}
