# Scaffolding created

`{{ data.id }}` — {{ data.name }}

{{ data.spec.templates | length }} template manifest{% if data.spec.templates | length != 1 %}s{% endif %}; bodies are available through `read_scaffolding_template`.
