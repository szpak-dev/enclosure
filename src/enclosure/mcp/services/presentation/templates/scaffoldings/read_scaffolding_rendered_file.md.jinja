# Rendered file `{{ data.path }}`

Revision `{{ data.revision }}`, characters {{ data.offset }}–{{ data.next_offset }} of {{ data.total_characters }}.

```text
{{ data.content }}
```
{% if data.has_more %}Continue with `read_scaffolding_rendered_file` at offset {{ data.next_offset }} using the same rendering parameters.{% endif %}
