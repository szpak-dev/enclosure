# {{ data.name }}

`{{ data.id }}` · {{ data.language_id }}

{{ data.description }}

Templates: {{ data.template_count }} at revision `{{ data.templates_revision }}`.

Use the typed follow-up to read template manifests, then `read_scaffolding_template` for an exact path and revision.
