# Scaffoldings

{% for scaffolding in data['items'] %}- `{{ scaffolding.id }}` — {{ scaffolding.name }} ({{ scaffolding.language_id }})
{% else %}- None
{% endfor %}{% if follow_ups %}
Use the supplied typed continuation to read the next scaffolding page.
{% endif %}
