# Scaffolding template manifests

Revision `{{ data.revision }}`, manifests {{ data.offset }}–{{ data.next_offset }} of {{ data.total }}.

{% for item in data.items %}
- `{{ item.path }}` — {{ item.size_bytes }} bytes, revision `{{ item.revision }}`
{% endfor %}
