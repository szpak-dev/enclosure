# Diagram command schema

`{{ data.kind }}.{{ data.operation }}` belongs to diagram-kind contract revision `{{ data.content_revision }}`.

The complete authoritative schema is available through the typed `read_diagram_kind_content` follow-up.
