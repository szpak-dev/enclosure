# Apply diagram command batch

Status: **ok**

Diagram command batch applied at the returned revision.

- Diagram: `{{ data.diagram_id }}`
- Revision: `{{ data.revision }}`
- Applied commands: `{{ data.applied_count }}`
