# Diagram content

- Diagram: `{{ properties.diagram_id }}`
- Document: `{{ properties.document }}`
- Revision: `{{ properties.revision }}`
- Characters: {{ properties.offset }}–{{ properties.next_offset }} of {{ properties.total_characters }}

```
{{ properties.content }}
```
{% if follow_ups %}
Use the supplied typed continuation to read the remaining diagram content.
{% endif %}
{% if data.actions | default([]) %}

## Available actions
{% for action in data.actions %}
- `{{ action.name }}` — {{ action.method }} `{{ action.href }}`
{% endfor %}
{% endif %}
{% if data.links | default([]) %}

## Links
{% for link in data.links %}
- {{ link.rel | join(", ") }} — `{{ link.href }}`
{% endfor %}
{% endif %}
