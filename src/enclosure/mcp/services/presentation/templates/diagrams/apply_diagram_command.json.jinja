{% set summary = "Diagram command applied at the returned revision." %}
{% from "shared/partials/envelope.json.jinja" import envelope with context %}
{{ envelope(operation_id, "ok", summary, data, []) }}
