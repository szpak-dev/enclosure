# Diagram-set diagram

`{{ data.id }}` — {{ data.title }} (`{{ data.kind }}`), revision {{ data.revision }}.

Source: {{ data.source_total_characters }} characters. Snapshot: {{ data.snapshot_total_characters }} characters.

Use the typed follow-ups to read the complete source or snapshot.
