# Diagram kind

`{{ data.id }}` — {{ data.name }}.

Canonical contract: {{ data.content_total_characters }} characters at revision `{{ data.content_revision }}`.

Use the typed follow-up to read the complete contract.
