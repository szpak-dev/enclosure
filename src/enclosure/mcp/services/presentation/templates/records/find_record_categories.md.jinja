# Record categories

{% for category in data['items'] %}
- `{{ category.id }}` — {{ category.title }} — schema version {{ category.schema_version }}
{% else %}
- None
{% endfor %}
{% if follow_ups %}

Use the supplied typed continuation to read the next page.
{% endif %}
