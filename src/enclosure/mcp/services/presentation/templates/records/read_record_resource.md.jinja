# Record resource content

Record: `{{ properties.record_id }}`
Path: `{{ properties.path }}`
Revision: `{{ properties.revision }}`
Characters: {{ properties.offset }}–{{ properties.next_offset }} of {{ properties.total_characters }}

```{{ properties.language }}
{{ properties.content }}
```
{% if follow_ups %}

Use the supplied typed continuation to read the remaining content.
{% endif %}
