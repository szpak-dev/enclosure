# Record category created

`{{ data.id }}` — {{ data.title }}

Schema version: {{ data.schema_version }}
Schema revision: `{{ data.content_schema_revision }}`
Schema bytes: {{ data.content_schema_size_bytes }}

Use `read_record_category_content_schema` with this category ID, schema version, and revision to read the schema.
