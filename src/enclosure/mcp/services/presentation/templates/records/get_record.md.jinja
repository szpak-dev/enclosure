# Record

`{{ data.id }}` — {{ data.title }}

Category: `{{ data.category.id }}` — {{ data.category.title }} (schema version {{ data.schema_version }})
{% if data.tags %}
Tags:{% for tag in data.tags %} `{{ tag.name }}`{% endfor %}
{% endif %}

Content: {{ data.content_total_characters }} characters at revision `{{ data.content_revision }}`.

Resources: {{ data.resource_count }} manifest{% if data.resource_count != 1 %}s{% endif %} at revision `{{ data.resources_revision }}`.

Use the typed follow-ups to read canonical content and resource manifests.
