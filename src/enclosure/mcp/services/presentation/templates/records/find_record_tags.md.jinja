# Record tags

{% for tag in data['items'] %}
- `{{ tag.id }}` — {{ tag.name }}
{% else %}
- None
{% endfor %}
{% if follow_ups %}

Use the supplied typed continuation to read the next page.
{% endif %}
