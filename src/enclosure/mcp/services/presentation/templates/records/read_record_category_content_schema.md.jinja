# Record category schema content

Category: `{{ properties.category_id }}`
Schema version: {{ properties.schema_version }}
Revision: `{{ properties.revision }}`
Characters: {{ properties.offset }}–{{ properties.next_offset }} of {{ properties.total_characters }}

```json
{{ properties.content }}
```
{% if follow_ups %}

Use the supplied typed continuation to read the remaining schema content.
{% endif %}
