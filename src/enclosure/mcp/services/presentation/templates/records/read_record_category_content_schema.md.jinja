# Record category schema content

Category: `{{ data.category_id }}`
Schema version: {{ data.schema_version }}
Revision: `{{ data.revision }}`
Characters: {{ data.offset }}–{{ data.next_offset }} of {{ data.total_characters }}

```json
{{ data.content }}
```
{% if follow_ups %}

Use the supplied typed continuation to read the remaining schema content.
{% endif %}
