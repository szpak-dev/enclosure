# Project health findings

{{ data.kind | title }} findings {{ data.offset }}–{{ data.next_offset }} of {{ data.total }}, revision `{{ data.revision }}`.

{% for finding in data.items %}
- **{{ finding.rule }}** — {{ finding.target }}: {{ finding.message }} Next: {{ finding.next_action }}
{% endfor %}
