# Project insight page

- Project: `{{ data.project_id }}`
- Workspace: `{{ data.workspace_id }}`
- Revision: `{{ data.revision }}`
- Path: `{{ data.path }}`
- Items: {{ data.offset }}–{{ data.next_offset }} of {{ data.total }}

{% for item in data["items"] %}
- `{{ item | tojson }}`
{% else %}
- None
{% endfor %}
{% if data.has_more %}

Continue with the bounded page operation at offset {{ data.next_offset }} using the same revision.
{% endif %}
{% include "projects/partials/navigation.md.jinja" %}
