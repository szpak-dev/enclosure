# Architecture configuration content

- Project: `{{ properties.project_id }}`
- Configuration: `{{ properties.configuration_id }}`
- Document: `{{ properties.document }}`
- Revision: `{{ properties.revision }}`
- Characters: {{ properties.offset }}–{{ properties.next_offset }} of {{ properties.total_characters }}

```yaml
{{ properties.content }}
```
{% if follow_ups %}
Use the supplied typed continuation to read the remaining configuration content.
{% endif %}
{% include "projects/partials/navigation.md.jinja" %}
