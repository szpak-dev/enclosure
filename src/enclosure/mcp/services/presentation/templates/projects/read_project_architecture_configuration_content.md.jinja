# Architecture configuration content

- Project: `{{ data.project_id }}`
- Configuration: `{{ data.configuration_id }}`
- Document: `{{ data.document }}`
- Revision: `{{ data.revision }}`
- Characters: {{ data.offset }}–{{ data.next_offset }} of {{ data.total_characters }}

```yaml
{{ data.content }}
```
{% if follow_ups %}
Use the supplied typed continuation to read the remaining configuration content.
{% endif %}
{% include "projects/partials/navigation.md.jinja" %}
