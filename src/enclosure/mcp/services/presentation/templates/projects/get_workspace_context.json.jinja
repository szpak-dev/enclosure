{% from "shared/partials/envelope.json.jinja" import envelope with context %}
{% set status = "ok" if data.readiness == "ready" else "error" %}
{% set summary = "Workspace context is ready." if data.readiness == "ready" else "Workspace context is incomplete." %}
{{ envelope(operation_id, status, summary, data, []) }}
