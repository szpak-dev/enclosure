# Architecture configuration

- Configuration: `{{ data.id }}`
- Project: `{{ data.project_id }}`
- Revision: `{{ data.revision }}`
- Boundaries: {{ data.boundaries_total_characters }} characters
- Shape: {{ data.shape_total_characters }} characters

Configuration YAML is intentionally omitted. Use the typed follow-ups to read either complete document.
{% include "projects/partials/navigation.md.jinja" %}
