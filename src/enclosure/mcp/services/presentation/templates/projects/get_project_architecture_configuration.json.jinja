{% from "shared/partials/envelope.json.jinja" import envelope with context %}
{{ envelope(operation_id, "ok", "Architecture configuration reference; content is available through bounded reads.", data, []) }}
